function soma(n1=0, n2=0){ //função e parâmtros
    return n1 + n2  //ação
}

console.log(soma(2)) //chamada, 2 e 5 são parâmetros

/*Caso em soma houvesse somente apenas 2 ou 5, seria retornada NaN, pois um dos valores seria undefined
para predefinir isso, no parâmetro formal (no inicio da função, colocamos n =0) opcionais*/